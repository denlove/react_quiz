const TextArea = () => {
    return (
        <textarea
            rows={5}
            cols={40}
            placeholder='Type the answer'
            maxLength={100}
        />
    )
}

export default TextArea
