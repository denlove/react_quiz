import styled from 'styled-components'

const StyledArticle = styled.article`
    width: 100%;
    flex-grow: 1;
    align-content: flex-end;
`

const TitleHeader = styled.header`
    display: flex;
    justify-content: space-between;
`

export { StyledArticle, TitleHeader }
