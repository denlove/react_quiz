import styled from 'styled-components'

const StyledTimer = styled.div`
    margin: 1rem 2rem;
`

const Time = styled.span`
    color: var(--text-color-6);
    font-size: var(--fs-25)
`

export { StyledTimer, Time }
