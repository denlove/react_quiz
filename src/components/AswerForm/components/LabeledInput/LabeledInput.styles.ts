import styled from 'styled-components'

const StyledLabel = styled.label`
    padding: 0.5rem;
    color: var(--text-color-4);
    font-size: var(--fs-20);
    user-select: none;

    input {
        width: 1rem;
        aspect-ratio: 1 / 1;
        margin: 0.5rem;
    }
`

export { StyledLabel }
